services:
  - type: web
    name: deadline-defender-ai
    env: node
    rootDir: web
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: GEMINI_API_KEY
        sync: false
